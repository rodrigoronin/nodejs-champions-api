export const findAllTeams = async (): Promise<unknown> => {
  // TODO: logic to retrieve all teams from the database and save into a list
  return [];
};

export const findTeamByID = async (id: number): Promise<unknown> => {
  // TODO: logic to retrieve one specific team from the team list (if it already exists)
  return {};
};
